public enum ItemType
{
    Equipment,
    Consumable,
    Resource,
}

public enum EquipableType
{
    Weapon,
    Shield,
    Mail,
}

public enum ConsumableType
{
    Attack,
    Armor,
    Health,
    Critical,
}